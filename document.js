Document property.


Screen


The window object also has a screen object with properties
Describing the physical display


Screen properties width and height are the full screen
Screen properties avail width and avail height omit the toolbarWindow 🌏


      Window is the main javascript object root,
The global object in a browser also can be treated as the root of the
Document object model. You can access it as a window.


Well, the window  is the first thing that gets loaded into the browser.
This window object has the majority of the properties like length, inner width
Inner height, name, it has been closed,its parents, and more.


Document
   
What about the document object then? The document object (DOM)
Is you html,aspx,php or other document that will be loaded into the
Browser. The document actually gets loaded inside the window object
And has properties
Available to it like title, URL,cookie etc.


What does this really mean?
That means if you want to access a property for the window it is
Window. Document property which is also available in short as









